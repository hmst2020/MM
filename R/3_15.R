T<-function(v){           # 自訂一函式用以計算線性轉換T
  return (c(v[1]-2*v[2],2*v[1]+v[2]))
}
(Te1<-T(c(1,0,0)))        # 標準基底e1線性轉換
(Te2<-T(c(0,1,0)))        # 標準基底e2線性轉換
(Te3<-T(c(0,0,1)))        # 標準基底e3線性轉換
(A<-cbind(Te1,Te2,Te3))   # R3到R2的標準線性轉換矩陣
#################end of 3_15.R#################
