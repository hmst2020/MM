########### 從標準至非標準基底的線性轉換###########
(B<-cbind(b1=c(1,2),b2=c(-1,1)))   # V 向量子空間的基底
(A1<-rbind(x1=c(1,1),x2=c(2,-1)))  # 標準轉換矩陣
(C<-cbind(c1=c(1,0),c2=c(1,1)))    # W 向量子空間的基底
solve(C)%*%(A1%*%B)                # 非標準基底V至W的轉換矩陣

################end of 3_16.R#######################



