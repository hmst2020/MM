T <-cbind(               # 建構遞移矩陣物件
  c(0.6,0.3,0.1),     
  c(0.4,0.3,0.3),
  c(0.3,0.3,0.4))
dimnames(T) <- list(     # 對矩陣給予列名、行名
  c('區域1','區域2','區域3'),c('區域1','區域2','區域3'))
print(T)           # a .列印遞移矩陣
(x0<-c(0.8,0.15,0.05)) # 初始計程車分布狀態
(x1 <- T %*% x0)   # b . 求第一回後分布狀態
(x2 <- T %*% x1)   # c . 求第二回後分布狀態
############### end of 8_4.R ############