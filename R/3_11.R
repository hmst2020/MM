u<-c(1,0,1)        # u 向量
v<-c(1,1,0)        # v 向量
w<-c(-1,1,1)       # w 向量
(S1<-cbind(u,v))     # S1子空間
(S2<-cbind(w))       # S2子空間
(t(S1)%*%S2)         # 驗證子空間為正交
###############end of 3_11.R ####################